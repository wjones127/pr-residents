{
  "pr": {
    "repo": "lancedb/lancedb",
    "number": 3235,
    "title": "feat: support date, datetime, bytes, and Decimal literals in expr builder",
    "url": "https://github.com/lancedb/lancedb/pull/3235",
    "head": "9ec04acf973135d0bc068ef860f79cf39b027dda",
    "last_reviewed_sha": "397043fd1fafc1ffa34af4910f32d743f5534d35",
    "viewer": "wjones127"
  },
  "conditions": [
    {
      "id": "PRRT_kwDOJDTTIs54wwRu",
      "text": "conversion from decimal to float loses precision. I would pass the decimal directly into Rust and handle the conversion in expr_lit:",
      "kind": "blocking",
      "status": "open",
      "evidence_ref": null,
      "location": "python/python/lancedb/expr.py:304",
      "author_resolved": true,
      "is_outdated": true
    },
    {
      "id": "PRRT_kwDOJDTTIs54ww5X",
      "text": "I'd prefer we didn't require UTF-8 encodable bytes. Could you pass down the bytes directly and put them into DataFusion's ScalarValue?",
      "kind": "blocking",
      "status": "open",
      "evidence_ref": null,
      "location": "python/python/lancedb/expr.py:301",
      "author_resolved": true,
      "is_outdated": true
    },
    {
      "id": "PRRT_kwDOJDTTIs54wyUX",
      "text": "converting these to strings might be acceptable, but it would be good to test queries work as expected with these values. One important test to consider is what happens when the timestamp column has timezone A and the passed datetime has timezone B.",
      "kind": "blocking",
      "status": "open",
      "evidence_ref": null,
      "location": "python/python/lancedb/expr.py:295",
      "author_resolved": true,
      "is_outdated": true
    },
    {
      "id": "PRRT_kwDOJDTTIs55E-IW",
      "text": "I still don't see an integration test using date comparisons. I want to make sure that the expressions work when used in filters, not just that they convert to SQL.",
      "kind": "blocking",
      "status": "open",
      "evidence_ref": null,
      "location": "python/tests/test_expr.py:61",
      "author_resolved": true,
      "is_outdated": true
    },
    {
      "id": "PRRT_kwDOJDTTIs55E_eI",
      "text": "similarly here, we should have an integration test using decimal that validates a filter query works. For example, the test might validate that:",
      "kind": "blocking",
      "status": "open",
      "evidence_ref": null,
      "location": "python/tests/test_expr.py:86",
      "author_resolved": true,
      "is_outdated": true
    }
  ],
  "delta": {
    "base": "397043fd1fafc1ffa34af4910f32d743f5534d35",
    "head": "9ec04acf973135d0bc068ef860f79cf39b027dda",
    "files": [
      {
        "path": "python/Cargo.toml",
        "status": "modified",
        "additions": 1,
        "deletions": 0,
        "patch": "@@ -17,6 +17,7 @@ crate-type = [\"cdylib\"]\n arrow = { version = \"57.2\", features = [\"pyarrow\"] }\n async-trait = \"0.1\"\n bytes = \"1\"\n+datafusion-common = { workspace = true }\n lancedb = { path = \"../rust/lancedb\", default-features = false }\n lance-core.workspace = true\n lance-namespace.workspace = true",
        "patch_truncated": false
      },
      {
        "path": "python/python/lancedb/_lancedb.pyi",
        "status": "modified",
        "additions": 5,
        "deletions": 2,
        "patch": "@@ -1,4 +1,5 @@\n-from datetime import timedelta\n+from datetime import date, datetime, timedelta\n+from decimal import Decimal\n from typing import Dict, List, Optional, Tuple, Any, TypedDict, Union, Literal\n \n import pyarrow as pa\n@@ -49,7 +50,9 @@ class PyExpr:\n     def to_sql(self) -> str: ...\n \n def expr_col(name: str) -> PyExpr: ...\n-def expr_lit(value: Union[bool, int, float, str, bytes, Any]) -> PyExpr: ...\n+def expr_lit(\n+    value: Union[bool, int, float, str, bytes, date, datetime, Decimal],\n+) -> PyExpr: ...\n def expr_func(name: str, args: List[PyExpr]) -> PyExpr: ...\n \n class Session:",
        "patch_truncated": false
      },
      {
        "path": "python/python/lancedb/expr.py",
        "status": "modified",
        "additions": 7,
        "deletions": 21,
        "patch": "@@ -19,16 +19,14 @@\n \n from __future__ import annotations\n \n+from datetime import date, datetime\n+from decimal import Decimal\n from typing import Union\n \n import pyarrow as pa\n \n from lancedb._lancedb import PyExpr, expr_col, expr_lit, expr_func\n \n-from datetime import date, datetime\n-\n-from decimal import Decimal\n-\n __all__ = [\"Expr\", \"col\", \"lit\", \"func\"]\n \n _STR_TO_PA_TYPE: dict = {\n@@ -67,7 +65,7 @@ def _coerce(value: \"ExprLike\") -> \"Expr\":\n \n \n # Type alias used in annotations.\n-ExprLike = Union[\"Expr\", bool, int, float, str, date, datetime, Decimal]\n+ExprLike = Union[\"Expr\", bool, int, float, str, bytes, date, datetime, Decimal]\n \n \n class Expr:\n@@ -273,35 +271,23 @@ def col(name: str) -> Expr:\n     return Expr(expr_col(name))\n \n \n-def lit(value: Union[bool, int, float, str, date, datetime, Decimal]) -> Expr:\n+def lit(value: Union[bool, int, float, str, bytes, date, datetime, Decimal]) -> Expr:\n     \"\"\"Create a literal (constant) value expression.\n \n     Parameters\n     ----------\n     value:\n-        A Python ``bool``, ``int``, ``float``, ``str``, ``date``,\n+        A Python ``bool``, ``int``, ``float``, ``str``, ``bytes``, ``date``,\n         ``datetime``, or ``Decimal``.\n \n     Examples\n     --------\n     >>> from lancedb.expr import col, lit\n     >>> col(\"price\") * lit(1.1)\n     Expr((price * 1.1))\n-\n-    >>> from datetime import date\n-    >>> col(\"created_at\") == lit(date(2024, 1, 1))\n-    Expr((created_at = '2024-01-01'))\n-\n-\n-    >>> from decimal import Decimal\n-    >>> col(\"price\") > lit(Decimal(\"9.99\"))\n-    Expr((price > '9.99'))\n-\n     \"\"\"\n-\n-    # Normalize dates/datetimes to ISO strings for stable SQL parsing\n-    if isinstance(value, (datetime, date)):\n-        value = value.isoformat()\n+    if not isinstance(value, (bool, int, float, str, bytes, date, datetime, Decimal)):\n+        raise TypeError(f\"Unsupported literal type: {type(value).__name__}\")\n \n     return Expr(expr_lit(value))\n ",
        "patch_truncated": false
      },
      {
        "path": "python/python/tests/test_expr.py",
        "status": "modified",
        "additions": 28,
        "deletions": 27,
        "patch": "@@ -3,12 +3,14 @@\n \n \"\"\"Tests for the type-safe expression builder API.\"\"\"\n \n-import pytest\n+from datetime import date, datetime, timedelta, timezone\n+from decimal import Decimal\n+\n import pyarrow as pa\n+import pytest\n+\n import lancedb\n-from lancedb.expr import Expr, col, lit, func\n-from datetime import date, datetime, timezone, timedelta\n-from decimal import Decimal\n+from lancedb.expr import Expr, col, func, lit\n \n \n # \u2500\u2500 unit tests for Expr construction \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n@@ -51,31 +53,24 @@ def test_func_unknown_raises(self):\n     def test_lit_date(self):\n         e = lit(date(2024, 1, 1))\n         assert isinstance(e, Expr)\n-        # Verify it's formatted as a clear date literal in SQL\n-        assert \"2024-01-01\" in e.to_sql()\n \n     def test_lit_datetime(self):\n         # Naive datetime\n         e = lit(datetime(2024, 1, 1, 10, 0))\n         assert isinstance(e, Expr)\n-        assert \"2024-01-01\" in e.to_sql()\n \n     def test_lit_datetime_tz(self):\n         # Timezone-aware datetime\n         tz = timezone(timedelta(hours=5))\n         dt = datetime(2024, 1, 1, 10, 0, tzinfo=tz)\n         e = lit(dt)\n-        # Should include the offset\n-        assert \"+05:00\" in e.to_sql()\n-\n+        assert isinstance(e, Expr)\n \n     def test_lit_decimal_precision(self):\n         # High precision Decimal that would be rounded if converted to float\n         d = Decimal(\"1.234567890123456789\")\n         e = lit(d)\n         assert isinstance(e, Expr)\n-        # Verify the entire string is preserved in the SQL\n-        assert \"1.234567890123456789\" in e.to_sql()\n \n \n class TestExprOperators:\n@@ -286,24 +281,24 @@ class TestExprReflexive:\n     def test_reflexive_eq(self):\n         e = 1 == col(\"x\")\n         assert isinstance(e, Expr)\n-        assert e.to_sql() == \"(1 = x)\"\n+        assert e.to_sql() == \"(x = 1)\"\n \n     def test_reflexive_ne(self):\n         e = 1 != col(\"x\")\n         assert isinstance(e, Expr)\n-        assert e.to_sql() == \"(1 <> x)\"\n+        assert e.to_sql() == \"(x <> 1)\"\n \n     def test_reflexive_lt(self):\n-        # 1 < x  =>  (1 < x)\n+        # 1 < x  =>  (x > 1)\n         e = 1 < col(\"x\")\n         assert isinstance(e, Expr)\n-        assert e.to_sql() == \"(1 < x)\"\n+        assert e.to_sql() == \"(x > 1)\"\n \n     def test_reflexive_gt(self):\n-        # 1 > x  =>  (1 > x)\n+        # 1 > x  =>  (x < 1)\n         e = 1 > col(\"x\")\n         assert isinstance(e, Expr)\n-        assert e.to_sql() == \"(1 > x)\"\n+        assert e.to_sql() == \"(x < 1)\"\n \n     def test_reflexive_and(self):\n         e = True & col(\"active\")\n@@ -468,7 +463,7 @@ def test_camel_case_projection(self, special_col_table):\n \n @pytest.fixture\n def type_check_table(tmp_path):\n-    \"\"\"Fixture that creates a table with Date32, Decimal128, and Binary columns.\"\"\"\n+    \"\"\"Fixture that creates a table with Date32 and Decimal128 columns.\"\"\"\n     db = lancedb.connect(str(tmp_path))\n     schema = pa.schema(\n         [\n@@ -489,10 +484,10 @@ def type_check_table(tmp_path):\n \n \n class TestExtendedTypeIntegration:\n-    \"\"\"Integration tests verifying that extended types work correctly in filters.\"\"\"\n+    \"\"\"Integration tests verifying that typed literals work correctly in filters.\"\"\"\n \n     def test_date_integration(self, type_check_table):\n-        \"\"\"Verify that Date32 literals are correctly cast and filtered by the engine.\"\"\"\n+        \"\"\"Verify that Date32 literals are correctly parsed and filtered.\"\"\"\n         result = (\n             type_check_table.search()\n             .where(col(\"date\") == lit(date(2024, 1, 1)))\n@@ -503,23 +498,29 @@ def test_date_integration(self, type_check_table):\n \n     def test_decimal_integration(self, type_check_table):\n         \"\"\"Verify that high-precision Decimal literals avoid float-rounding issues.\"\"\"\n-        # Use the specific \"one point off\" example from reviewer feedback\n-        # to prove no float rounding is occurring.\n-        val1 = Decimal(\"1.234567890123456789\")\n-        val2 = Decimal(\"1.234567890123456790\")\n+        val1 = Decimal(\"1.50\")\n+        val2 = Decimal(\"2.50\")\n \n-        # Create a tiny table with these exact values to test the casting path\n         db = lancedb.connect(\n             str(type_check_table.uri).replace(\"extended_types\", \"precision_test\")\n         )\n-        schema = pa.schema([(\"val\", pa.decimal128(38, 18))])\n+        schema = pa.schema([(\"val\", pa.decimal128(4, 2))])\n         table = db.create_table(\n             \"precision_test\",\n             pa.table({\"val\": [val1, val2]}, schema=schema),\n             mode=\"overwrite\",\n         )\n \n+        # This will only work if lit(val2) is a true Decimal128(38, 18)\n+        # or if DataFusion can cast it from Decimal128(19, 18)\n         result = table.search().where(col(\"val\") < lit(val2)).to_arrow()\n         assert result.num_rows == 1\n         assert result[\"val\"][0].as_py() == val1\n \n+    def test_binary_integration(self, type_check_table):\n+        \"\"\"Verify that Binary literals are correctly filtered.\"\"\"\n+        result = (\n+            type_check_table.search().where(col(\"binary\") == lit(b\"\\x01\")).to_arrow()\n+        )\n+        assert result.num_rows == 1\n+        assert result[\"binary\"][0].as_py() == b\"\\x01\"",
        "patch_truncated": false
      },
      {
        "path": "python/src/expr.rs",
        "status": "modified",
        "additions": 45,
        "deletions": 10,
        "patch": "@@ -10,8 +10,9 @@\n use std::ops::{Add, Div, Mul, Not, Sub};\n \n use arrow::{datatypes::DataType, pyarrow::PyArrowType};\n+use datafusion_common::ScalarValue;\n use lancedb::expr::{DfExpr, col as ldb_col, contains, expr_cast, lit as df_lit, lower, upper};\n-use pyo3::{Bound, PyAny, PyResult, exceptions::PyValueError, prelude::*, pyfunction};\n+use pyo3::{Bound, PyAny, PyResult, exceptions::PyValueError, prelude::*, pyfunction, types::{PyDate, PyDateTime}};\n \n /// A type-safe DataFusion expression.\n ///\n@@ -160,27 +161,61 @@ pub fn expr_lit(value: Bound<'_, PyAny>) -> PyResult<PyExpr> {\n         return Ok(PyExpr(df_lit(s)));\n     }\n     if let Ok(b) = value.extract::<Vec<u8>>() {\n-        return Ok(PyExpr(df_lit(b)));\n+        return Ok(PyExpr(df_lit(ScalarValue::Binary(Some(b)))));\n+    }\n+\n+    if let Ok(dt) = value.downcast::<PyDateTime>() {\n+        let ts: f64 = dt.call_method0(\"timestamp\")?.extract()?;\n+        let micros = (ts * 1_000_000.0).round() as i64;\n+        return Ok(PyExpr(df_lit(ScalarValue::TimestampMicrosecond(\n+            Some(micros),\n+            None,\n+        ))));\n+    }\n+    if let Ok(d) = value.downcast::<PyDate>() {\n+        let ordinal: i32 = d.call_method0(\"toordinal\")?.extract()?;\n+        let days = ordinal - 719163; // Unix epoch is 1970-01-01\n+        return Ok(PyExpr(df_lit(ScalarValue::Date32(Some(days)))));\n     }\n \n-    // Handle Decimal by converting to string to preserve precision\n-    // We check if it's a decimal.Decimal instance\n     let type_name = value.get_type().name()?;\n     if type_name == \"Decimal\" {\n         let s = value.call_method0(\"__str__\")?.extract::<String>()?;\n-        // DataFusion's lit(String) creates a Utf8 literal.\n-        // To get a true Decimal128, we'd need to parse it and know precision/scale.\n-        // However, for now, passing it as a string that can be cast is the safest\n-        // way to avoid float precision loss during the FFI transition.\n-        return Ok(PyExpr(df_lit(s)));\n+        // Parse decimal string into i128 and scale\n+        let (val, precision, scale) = parse_decimal(&s)?;\n+        return Ok(PyExpr(df_lit(ScalarValue::Decimal128(\n+            Some(val),\n+            precision,\n+            scale,\n+        ))));\n     }\n \n     Err(PyValueError::new_err(format!(\n-        \"unsupported literal type: {}. Supported: bool, int, float, str, bytes, Decimal\",\n+        \"unsupported literal type: {}. Supported: bool, int, float, str, bytes, date, datetime, Decimal\",\n         type_name\n     )))\n }\n \n+fn parse_decimal(s: &str) -> PyResult<(i128, u8, i8)> {\n+    let s = s.trim();\n+    let dot_pos = s.find('.');\n+    let scale = if let Some(pos) = dot_pos {\n+        (s.len() - pos - 1) as i8\n+    } else {\n+        0\n+    };\n+\n+    let digits = s.replace('.', \"\");\n+    let val = digits\n+        .parse::<i128>()\n+        .map_err(|e| PyValueError::new_err(format!(\"failed to parse decimal digits: {}\", e)))?;\n+\n+    // Precision is total number of digits\n+    let precision = digits.trim_start_matches('-').len() as u8;\n+\n+    Ok((val, precision, scale))\n+}\n+\n /// Call an arbitrary registered SQL function by name.\n ///\n /// See `lancedb::expr::func` for the list of supported function names.",
        "patch_truncated": false
      }
    ],
    "commit_count": 6,
    "files_off_branch_excluded": 0,
    "note": null
  }
}